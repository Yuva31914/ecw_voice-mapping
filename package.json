{
  "name": "clinical-voice-assistant",
  "version": "1.0.0",
  "description": "Clinical Voice Assistant with OAuth, AI transcription, and deployment setup",
  "private": true,
  "scripts": {
    "build": "cd mini-frontend && npm install && npx vite build",
    "dev": "cd mini-frontend && npm run dev"
  }
}
