{
  "name": "ecw-voice-mapping",
  "version": "1.0.0",
  "scripts": {
    "build": "cd mini-frontend && npm install && npm run build",
    "dev": "cd mini-frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}